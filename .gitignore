# OS noise
.DS_Store

# Build artifacts / deploy bundles (regenerated; don't track)
Vivo_deploy/
Vivo_site/
*.zip

# Design-source material — kept inside the repo folder for reference,
# but not shipped or version-controlled (mood boards, brand PDFs, raw
# mockups, and the superseded static prototype). Fonts used in
# production now live in assets/fonts/ and ARE tracked.
design-source/
batchvideo.mp4
*.pdf
Vivo Mood Board Images/

# Node / Medusa (for when the backend dev adds tooling)
node_modules/
.env
.env.*
dist/
.netlify/
